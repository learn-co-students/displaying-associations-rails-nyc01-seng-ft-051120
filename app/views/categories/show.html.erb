<h1><%= @category.name %></h1>
<h3><%= pluralize(@category.posts.count, 'posts') %></h3>
<ul>
<% @category.posts.each do |v|%>
<li></li><%=link_to v.title, post_path(v)  %></li>
<% end%>
</ul>
<!-- Add code here to list a category's posts. -->
